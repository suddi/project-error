machine:
    timezone:
        Asia/Hong_Kong
    node:
        version: '8.9.1'
test:
    override:
        - source ./bin/ci.sh:
            parallel: true
    post:
        - bash <(curl -s https://codecov.io/bash)
notify:
    webhooks:
        - url: https://bot.suddi.io/hooks/circle
